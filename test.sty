%% =========================
%% タイトル関連の定義
%% =========================

%% --- 寸法（タイトルページ内のレイアウト制御） ---
\newdimen\vspacetitle
\newdimen\vspaceabstract
\vspacetitle=0.5\textheight
\vspaceabstract=0.5\textheight

%% --- メタ情報（\maketitle が参照する内部変数のセット） ---
% \def\etitle#1{\gdef\@etitle{#1}} % 使っていない場合はこのまま保持
\def\degree#1{\gdef\@degree{#1}} % Added Nov. 2012
\def\advisor#1{\gdef\@advisor{#1}}
\def\majorfield#1{\gdef\@majorfield{#1}}
\def\programfield#1{\gdef\@programfield{#1}}
\def\abstract#1{\gdef\@abstract{#1}}
\def\yearandmonth#1{\gdef\@yearandmonth{#1}}
\def\keywords#1{\gdef\@keywords{#1}}

\def\keywordsname{\bf Key Words : }

%% =========================
%% \maketitle 本体
%% =========================
\def\maketitle{\par
\begingroup
  \def\@makefnmark{\hbox to\z@{$\m@th^{\@thefnmark}$\hss}}%
  \if@twocolumn
    \twocolumn[\@maketitle]
  \else
    %\newpage
    \global\@topnum\z@
    \@maketitle
  \fi
  \thispagestyle{plain}\@thanks
\endgroup

%% 再定義防止・後処理（原文の挙動維持）
\let\maketitle\relax
\let\@maketitle\relax
\gdef\@abstract{}\gdef\@keywords{}
\let\abstract\relax \let\keywords\relax
\let\etitle\relax\gdef\@etitle{}
\let\majorfield\relax\gdef\@majorfield{}
\let\programfield\relax\gdef\@programfield{}
\gdef\@thanks{}\gdef\@author{}\gdef\@title{}\let\thanks\relax
}

%% =========================
%% タイトルページのレイアウト定義
%% =========================
\def\@maketitle{
\newpage\null
\vbox to \vspacetitle{
\thispagestyle{empty}

  %\fbox{ % 枠を付けてレイアウト確認したい場合に有効化
  \begin{minipage}{6in}
    \begin{center}
      \vspace*{1cm}
      {\huge 令和 7 年度}

      \vspace{5mm}
      {\huge 筑波大学理工学群社会工学類}

      \vspace{5mm}
      {\huge 卒業研究論文}

      \vspace{2.4cm}
      {\huge \baselineskip 1.5em \@title\\} \vfill

      \vspace{5cm plus 2cm minus 2cm}

      {\Large 経営工学主専攻}

      \vspace{5mm}
      {\Large 学籍番号：202110892}  % New Nov. 2012

      \vspace{5mm}
      {\Large 野坂健成 }

      \vspace{5mm}
      {\Large 指導教員：高野祐一 准教授}

      \vspace{1.5cm}
      {\Large \@yearandmonth}

      %\vspace{2cm}
      ~
    \end{center}
  \end{minipage}
  %} % end of fbox

%% -------------------------
%% 概要・キーワード（現在は不使用：原文コメントアウトを保持）
%% タイトルページ内に要約を出したい場合は、下のブロックを復活させる。
%% -------------------------
% \vbox to \vspaceabstract{\vfill
% \@ifundefined{@abstract}{}{
% \begin{list}{}{\setlength{\leftmargin}{0.15\textwidth}
% \topsep=0pt \partopsep=0pt \listparindent=1em \parsep=0pt
% \setlength{\rightmargin}{\leftmargin}
% \setlength{\baselineskip}{0.8\normalbaselineskip}}
% \item {\small \@abstract}
% \@ifundefined{@keywords}{}{
% \begin{list}{\keywordsname}{
% \topsep=-0.8\parskip \partopsep=0pt
% \settowidth{\labelwidth}{\keywordsname}
% \settowidth{\leftmargin}{1.1\keywordsname}
% \setlength{\baselineskip}{0.8\normalbaselineskip}}
% \item {\small\it\@keywords}
% \end{list}}
% \end{list}}
% \par \vfill}
}
}